#!/home/miles/bin/setR

library(utils)
library(graphics)
first <- TRUE
for ( file in argv ) {
	var = read.table(file, header=TRUE)
	if ( first ) {
		plot(var)
		first <- FALSE
	} else {
		points(var)
	}
}
readline("Press <Enter> to continue")
q("no")
